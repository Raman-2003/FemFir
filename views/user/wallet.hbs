<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Wallet</title>
    <link rel="stylesheet" href="https://maxcdn.bootstrapcdn.com/bootstrap/4.5.2/css/bootstrap.min.css">
    <script src="https://cdn.jsdelivr.net/npm/sweetalert2@11"></script>
    <style>
        body {
            background-color: #f8f9fa;
            font-family: 'Arial', sans-serif;
        }

        .dashboard-menu {
            margin-bottom: 30px;
        }

        .dashboard-menu li {
            display: inline;
            margin: 0 10px;
        }

        .dashboard-menu li a {
            padding: 10px 20px;
            border-radius: 5px;
            background-color: #165292;
            color: #fff;
            text-decoration: none;
            transition: background-color 0.3s ease;
        }

        .dashboard-menu li a:hover,
        .dashboard-menu li a.active {
            background-color: #0056b3;
        }

        .dashboard-wrapper {
            padding: 30px;
            background-color: #ffffff;
            box-shadow: 0px 0px 10px rgba(0, 0, 0, 0.1);
            border-radius: 10px;
        }

        .wallet-summary {
            text-align: center;
            margin-bottom: 30px;
        }

        .wallet-summary h2 {
            font-size: 2rem;
            color: #333;
            margin-bottom: 20px;
        }

        .wallet-summary .btn {
            font-size: 1rem;
            padding: 10px 20px;
        }

        .table-responsive {
            margin-top: 20px;
        }

        .table thead {
            background-color: #206dad;
            color: #fff;
        }

        .table thead th {
            font-weight: bold;
        }

        .table tbody tr:hover {
            background-color: #f1f1f1;
        }

        .table td {
            vertical-align: middle;
        }

        .filter-form {
            margin-bottom: 20px;
        }

        .filter-form h3 {
            margin-bottom: 15px;
        }

        .filter-form label {
            margin-right: 10px;
        }

        .filter-form .form-group {
            margin-bottom: 10px;
        }

        .filter-form .btn {
            background-color: #165292;
            color: #fff;
        }

        .wallet-summary-hr {
            border: 0;
            height: 1px;
            background-color: #333;
            margin-top: 10px;
        }

        .filter-form {
            display: flex;
            flex-direction: column;
            align-items: flex-end;
            margin-bottom: 20px;
        }

       
        .form-inline {
            display: flex;
            flex-direction: column;
            align-items: flex-end;
            width: 100%;
        }

       

        
    </style>
</head>

<body>
    <section class="user-dashboard page-wrapper">
        <div class="container">
            <div class="row">
                <div class="col-md-12">
                    <div class="dashboard-wrapper user-dashboard">
                        <div class="wallet-summary">
                            <h2>Wallet Balance: ₹{{wallet.balance}}</h2>
                            <a href="/wallet/add-money" style="color: #0056b3;">Add Money</a>
                        </div>
                        <hr class="wallet-summary-hr"> 

                        <div class="filter-form">
                            <h3 class="sort-header">Sort Transactions</h3>
                            <form action="/wallet" method="get" class="form-inline">
                                <div class="form-group sort-group">
                                    <label for="sort" class="sort-label">Sort by:</label>
                                    <select id="sort" name="sort" class="form-control">
                                        <option value="">Select</option>
                                        <option value="maxAmount">Maximum Price</option>
                                        <option value="minAmount">Minimum Price</option>
                                        <option value="oldDate">Old Date</option>
                                        <option value="newDate">New Date</option>
                                    </select>
                                </div>
                                <div class="form-group">
                                    <button type="submit" class="btn">Apply Sort</button>
                                </div>
                            </form>
                        </div>


                        <div class="table-responsive">
                            <h3>Transaction History</h3>
                            <table class="table">
                                <thead>
                                    <tr>
                                        <th>Date</th>
                                        <th>Amount</th>
                                        <th>Note</th>
                                        <th>Status</th>
                                    </tr>
                                </thead>
                                <tbody>
                                    {{#each transactions}}
                                    <tr>
                                        <td>{{formatDate this.createdAt}}</td>
                                        <td>₹{{this.amount}}</td>
                                        <td>{{this.description}}</td>
                                        <td>{{this.type}}</td>
                                    </tr>
                                    {{/each}}
                                </tbody>
                            </table>
                        </div>
                    </div>
                </div>
            </div>
        </div>
    </section>
</body>

</html>